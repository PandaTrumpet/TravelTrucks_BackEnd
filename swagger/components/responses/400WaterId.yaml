description: Invalid Id
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Invalid Id
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: "Invalid Water ID format"
