description: Not found
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 404
        message:
          type: string
          example: Not found
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: "User with id 67719abf3fc799eedfd041ba not found`"
